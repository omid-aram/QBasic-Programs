DECLARE SUB help1 ()
DECLARE SUB help ()
COMMON flag
IF flag = 0 THEN GOTO 10
IF flag = 1 THEN KILL "rtab6.bes": END
IF flag = 2 THEN GOTO 20
10 CLS : SCREEN 0
20 CALL help: COLOR 14: LINE INPUT "Your Function : y = "; f$: COLOR 7
30 CALL help1: COLOR 14: LINE INPUT "Domain of your Function = "; Df$: COLOR 7
OPEN "rtab6.bes" FOR OUTPUT AS #1
PRINT #1, "common flag"
PRINT #1, "ON ERROR GOTO ert"
PRINT #1, " pix = 1: j = 1: Xm1 = 1: zx = 0: zx1 = 1:ste=j:cls"
PRINT #1, "INPUT" + CHR$(34) + "The graph should start from " + CHR$(34); "; aa"
PRINT #1, "INPUT " + CHR$(34) + "The graph should stop on " + CHR$(34); "; aa1"
PRINT #1, " IF aa > aa1 THEN SWAP aa, aa1"
PRINT #1, " a = aa: a1 = aa1"
PRINT #1, " DEF fnf (x) = " + f$
PRINT #1, "''''''''''"
PRINT #1, "70 gosub hesab"
PRINT #1, "''''''''''"
PRINT #1, "80 gosub rasm"
PRINT #1, "''''''''''"
PRINT #1, " WHILE ak$ <> CHR$(27)"
PRINT #1, " ak$ = INKEY$"
PRINT #1, " IF ak$ = CHR$(43) THEN pix = pix + 1: GOTO 80"
PRINT #1, " IF ak$ = CHR$(45) THEN"
PRINT #1, "  pix = pix - 1: IF pix = 0 THEN pix = 1"
PRINT #1, "  GOTO 70"
PRINT #1, " END IF"
PRINT #1, " IF ak$ = CHR$(46) THEN pix = 1: GOTO 70"
PRINT #1, " IF ak$ = CHR$(48) THEN j = 1: a = aa: a1 = aa1: pix = 1: GOTO 70"
PRINT #1, " IF ak$ = CHR$(42) THEN a1 = a1 / 2: a = a / 2: GOTO 70"
PRINT #1, " IF ak$ = CHR$(47) THEN a1 = a1 * 2: a = a * 2: GOTO 70"
PRINT #1, " IF ak$ = CHR$(99) OR ak$ = CHR$(67) THEN SWAP a, a1: SWAP x001, x002: j = j * -1: GOTO 80"
PRINT #1, " IF ak$ = CHR$(90) OR ak$ = CHR$(122) THEN SWAP zx, zx1: GOTO 70"
PRINT #1, " IF ak$ = CHR$(50) THEN GOSUB 1100: k01 = 8: GOSUB 1200: y00 = y00 - 1: k00 = POINT(x00, y00): GOSUB 1000: IF n000 MOD 2 <> 0 THEN GOSUB 1200"
PRINT #1, " IF ak$ = CHR$(119) OR ak$ = CHR$(87) THEN ste = j * (Xm1 / 320): GOTO 70"
PRINT #1, " IF ak$ = CHR$(52) THEN GOSUB 1100: k01 = 8: GOSUB 1200: x00 = x00 - 1: k00 = POINT(x00, y00): GOSUB 1000: IF n000 MOD 2 <> 0 THEN GOSUB 1200"
PRINT #1, " IF ak$ = CHR$(54) THEN GOSUB 1100: k01 = 8: GOSUB 1200: x00 = x00 + 1: k00 = POINT(x00, y00): GOSUB 1000: IF n000 MOD 2 <> 0 THEN GOSUB 1200"
PRINT #1, " IF ak$ = CHR$(56) THEN GOSUB 1100: k01 = 8: GOSUB 1200: y00 = y00 + 1: k00 = POINT(x00, y00): GOSUB 1000: IF n000 MOD 2 <> 0 THEN GOSUB 1200"
PRINT #1, " IF ak$ = CHR$(53) THEN"
PRINT #1, "  n000 = n000 + 1"
PRINT #1, "  IF n000 MOD 2 <> 0 THEN x001 = x00: y001 = y00"
PRINT #1, "  IF n000 MOD 2 = 0 THEN x002 = x00: y002 = y00: IF x001 <> x002 OR y001 <> y002 THEN gosub xcop"
PRINT #1, " END IF"
PRINT #1, " LOCATE 1, 1: PRINT " + CHR$(34) + "(" + CHR$(34) + "; x00; " + CHR$(34) + "," + CHR$(34) + "; y00; " + CHR$(34) + ") " + CHR$(34); ""
PRINT #1, "WEND"
PRINT #1, "print" + CHR$(34) + "Do you want to try again ?(Press <N> to exit)" + CHR$(34); " : ak$=input$(1):if ucase$(ak$)=chr$(78) then flag=1 else flag=0"
PRINT #1, "chain" + CHR$(34) + "rtab0.bas" + CHR$(34)
PRINT #1, "end"
PRINT #1, "1000 PSET (x00, y00), 14: RETURN"
PRINT #1, "1100 PSET (x00, y00), k00: RETURN"
PRINT #1, "1200 LINE (x001, y001)-(x00, y00), k01, B: k01 = 10: LINE (-Xm, 0)-(Xm, 0): LINE (0, -Ym)-(0, Ym): RETURN"
PRINT #1, "Dfg:IR = x = x: IZ = x = INT(x): IN = x = INT(x) AND x > 0: IW = x = INT(x) AND x >= 0: ISqr = x >= 0: Pi = 4 * ATN(1): Df ="; Df$; " :return"
PRINT #1, "hesab: Ym = 0: FOR x0 = a TO a1 STEP ste: x = x0: GOSUB Dfg"
PRINT #1, " IF Df = -1 THEN"
PRINT #1, "  y = ABS(fnf(x))"
PRINT #1, "  IF y > Ym THEN Ym = y"
PRINT #1, " END IF: NEXT x0: Xm = 320 * Ym / 240"
PRINT #1, " IF ABS(a) >= ABS(a1) THEN a2 = ABS(a) ELSE a2 = ABS(a1)"
PRINT #1, " IF a2 > Xm THEN Xm = a2: Ym = 240 * Xm / 320"
PRINT #1, " IF Xm1 <> 0 THEN CLS : SCREEN 12: WINDOW (-Xm, -Ym)-(Xm, Ym)"
PRINT #1, " GOSUB mehvar: Xm1 = Xm:ste=j:return"
PRINT #1, "mehvar: FOR l = 0 TO Ym: LINE (-Xm, l)-(Xm, l), 8: LINE (-Xm, -l)-(Xm, -l), 8: NEXT l: FOR l = 0 TO Xm: LINE (l, -Ym)-(l, Ym), 8: LINE (-l, -Ym)-(-l, Ym), 8: NEXT l: LINE (-Xm, 0)-(Xm, 0): LINE (0, -Ym)-(0, Ym):return"
PRINT #1, "rasm:FOR x0 = a TO a1 STEP j * (Xm1 / 320) / pix: x = x0: GOSUB Dfg: IF Df = -1 THEN PSET (x, fnf(x)), 12: GOTO 52"
PRINT #1, " NEXT x0"
PRINT #1, "52 FOR x0 = a TO a1 STEP j * (Xm1 / 320) / pix: x = x0: GOSUB Dfg"
PRINT #1, " if zx=0 then IF Df = -1 THEN PSET (x, fnf(x)), 12"
PRINT #1, " IF zx = 1 THEN IF Df = -1 THEN LINE -(x, fnf(x)), 12"
PRINT #1, " NEXT x0: RETURN"
PRINT #1, "xcop: IF x001 > x002 THEN SWAP x001, x002"
PRINT #1, "IF y001 > y002 THEN SWAP y001, y002"
PRINT #1, "IF j = -1 THEN SWAP x001, x002"
PRINT #1, "x000 = x002 - x001: y000 = y002 - y001"
PRINT #1, "IF x000 >= (4 / 3) * y000 THEN"
PRINT #1, " yn00 = x000 * (3 / 4): yk00 = (yn00 - y000) / 2: CLS : SCREEN 12: WINDOW (x001, y001 - yk00)-(x002, y002 + yk00)"
PRINT #1, "END IF"
PRINT #1, "IF x000 < (4 / 3) * y000 THEN"
PRINT #1, " xn00 = y000 * (4 / 3): xk00 = (xn00 - x000) / 2: CLS : SCREEN 12: WINDOW (x001 - xk00, y001)-(x002 + xk00, y002)"
PRINT #1, "END IF"
PRINT #1, " a = x001: a1 = x002: Xm1 = 0"
PRINT #1, "GOSUB hesab: LINE (x001, y001)-(x002, y002), 10, B: Xm1 = x002 - x001 + (2 * xk00): GOSUB rasm: RETURN"
PRINT #1, "ert: PRINT" + CHR$(34) + "ERROR" + CHR$(34) + "ERR :print " + CHR$(34) + "TRY AGAIN,PLEASE" + CHR$(34) + "flag=2:chain" + CHR$(34) + "rtab0.bas" + CHR$(34)
PRINT #1, "function ASN(x)"
PRINT #1, "cx = SQR(1 - (x) ^ 2)"
PRINT #1, "IF cx = 0 THEN ASN=1.57: goto 300"
PRINT #1, "tx = x / cx: ASN = ATN(tx)"
PRINT #1, "300 end function"
PRINT #1, "function ACS(x)"
PRINT #1, "PI=4*atn(1):if x=0 then ACS=1.57:goto 400"
PRINT #1, "sx = SQR(1 - (x) ^ 2):tx = sx / x:"
PRINT #1, "if x<0 then ACS = ATN(tx)+PI else acs=atn(tx)"
PRINT #1, "400 end function"

CLOSE #1
RUN "rtab6.bes"

SUB help
PRINT CHR$(34) + "+" + CHR$(34); " = +        " + CHR$(34) + "-" + CHR$(34); " = -        " + CHR$(34) + "*" + CHR$(34); " = x        " + CHR$(34) + "/" + CHR$(34); " = ö        " + CHR$(34) + "Enter" + CHR$(34); " ¯ =        "
PRINT "-------------------------------------------------------------------------------"
PRINT CHR$(34) + "SQR(x)" + CHR$(34); " = ûx     " + CHR$(34) + "x^n" + CHR$(34); " = xü      " + CHR$(34) + "PI" + CHR$(34); " = ã          " + CHR$(34) + "1/TAN(x)" + CHR$(34); " = Cot(x)   "
PRINT "-------------------------------------------------------------------------------"
PRINT CHR$(34) + "SIN(x)" + CHR$(34); " = Sin(x)         " + CHR$(34) + "COS(x)" + CHR$(34); " = Cos(x)         " + CHR$(34) + "TAN(x)" + CHR$(34); " = Tan(x)         "
PRINT "-------------------------------------------------------------------------------"
PRINT CHR$(34) + "ASN(x)" + CHR$(34); " = Arcsin(x)      " + CHR$(34) + "ACS(x)" + CHR$(34); " = Arccos(x)      " + CHR$(34) + "ATN(x)" + CHR$(34); " = Arctan(x)      "
PRINT "-------------------------------------------------------------------------------"
PRINT CHR$(34) + "ABS(x)" + CHR$(34); " = ³x³            " + CHR$(34) + "INT(x)" + CHR$(34); " = [x]            " + CHR$(34) + "SGN(x)" + CHR$(34); " = Sign(x)        "
PRINT "-------------------------------------------------------------------------------"
PRINT CHR$(34) + "LOG(x)/LOG(10)" + CHR$(34); " = Log(x)      " + CHR$(34) + "LOG(x)" + CHR$(34); " = Ln(x)         " + CHR$(34) + "EXP(x)" + CHR$(34); " = e^x        "
PRINT "-------------------------------------------------------------------------------"

END SUB

SUB help1
PRINT "-------------------------------------------------------------------------------"
PRINT "IR = (-ì , ì)   IZ = {...,-2,-1,0,1,2,...}    IW = {0,1,2,...}   IN = {1,2,...}"
PRINT "-------------------------------------------------------------------------------"
PRINT "Example:   R - {a,b}       ¯¯  NOT (x = a AND x = b)"
PRINT "           R - [a,b)       ¯¯  NOT (x >= a AND x < b)"
PRINT "           (a,b]           ¯¯  (x > a and x <= b)"
PRINT "           R - Z           ¯¯  IR AND NOT IZ"
PRINT "           (a,b) U (c,d)   ¯¯  (x>a AND x<b) OR (x>c AND x<d)"
PRINT "           (a,b) ï (c,d)   ¯¯  (x>a AND x<b) AND (x>c AND x<d)"
PRINT "-------------------------------------------------------------------------------"
END SUB

